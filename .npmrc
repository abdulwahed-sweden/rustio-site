# Use a project-local npm cache so `npm install` works even when the global
# ~/.npm cache has permission issues (a common macOS bug: "cache folder
# contains root-owned files"). Removes the need for sudo to install here.
cache=.npm-cache
fund=false
audit=false
